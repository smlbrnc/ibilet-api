# ==============================================
# iBilet Internal Core API - Environment Template
# ==============================================
# Bu dosyayı kopyalayın ve .env.development veya .env.production olarak kaydedin
# Ardından değerleri kendi ortamınıza göre düzenleyin

# Uygulama Ayarları
NODE_ENV=development
API_URL=https://api-dev.ibilet.com
PORT=3000

# PAX API (Paximum) Ayarları
# Base URL: Paximum Stage API endpoint'i
PAX_BASE_URL=http://service.stage.paximum.com/v2/api
# Agency: Paximum acenta kodu (kendi kodunuzu girin)
PAX_AGENCY=YOUR_AGENCY_CODE
# User: Paximum API kullanıcı adı (kendi kullanıcı adınızı girin)
PAX_USER=YOUR_USERNAME
# Password: Paximum API şifresi (kendi şifrenizi girin)
PAX_PASSWORD=YOUR_PASSWORD

# JWT (JSON Web Token) Ayarları
# Secret: Token imzalama için kullanılan gizli anahtar
# ÖNEMLİ: Production'da güçlü, rastgele bir anahtar kullanın!
# Örnek oluşturma: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
# Expires In: Token geçerlilik süresi (örnekler: 1h, 7d, 30d, 90d)
JWT_EXPIRES_IN=7d

# CORS (Cross-Origin Resource Sharing) Ayarları
# Origins: API'ye erişim izni verilen domain'ler (virgülle ayrılmış)
# Development: localhost + test domain'leri
# Production: Sadece gerçek domain'ler
CORS_ORIGINS=http://localhost:3001,https://app-dev.ibilet.com

# Rate Limiting (Hız Sınırlama) Ayarları
# TTL: Time to live - Zaman penceresi (saniye cinsinden, 60 = 1 dakika)
THROTTLE_TTL=60
# Limit: Belirtilen zaman penceresinde maksimum istek sayısı
# 100 = Dakikada 100 istek
THROTTLE_LIMIT=100
